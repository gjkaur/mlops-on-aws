# Seed code nib (Lab 10)

Purpose: illustrate a **minimal** **`pytest`** smoke test + **CodeBuild** `buildspec` fragment instructors can splice into cloned **SageMaker Project** repos when the bundled sample is flaky or empty.

**Do not overwrite** upstream training/pipeline helpers blindly—mirror the repo’s packaging layout first.
